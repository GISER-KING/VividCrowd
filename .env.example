# ============================================
# VividCrowd 环境变量配置示例
# ============================================
#
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填入实际的配置值
# 3. 不要将 .env 文件提交到 Git
#

# -------------------- 必需配置 --------------------
# 阿里云 DashScope API Key（必需）
# 申请地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your-dashscope-api-key-here

# -------------------- 数字人功能（可选）--------------------
# 火山引擎配置（用于数字人视频生成）
# 如果不使用数字人视频功能，可以不配置
# VOLCENGINE_ACCESS_KEY=your-volcengine-access-key
# VOLCENGINE_SECRET_KEY=your-volcengine-secret-key

# 阿里云 OSS 配置（用于视频文件存储）
# 如果不使用数字人视频功能，可以不配置
# OSS_ACCESS_KEY_ID=your-oss-access-key-id
# OSS_ACCESS_KEY_SECRET=your-oss-access-key-secret
# OSS_BUCKET_NAME=your-bucket-name
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com

# -------------------- 服务器配置（可选）--------------------
# 监听地址（默认：0.0.0.0）
# HOST=0.0.0.0

# 监听端口（默认：8000）
# PORT=8000

# 日志级别（DEBUG/INFO/WARNING/ERROR，默认：INFO）
# LOG_LEVEL=INFO

# 热重载（开发环境可设为 True，生产环境建议 False）
# RELOAD=False

# -------------------- 数据库配置（可选）--------------------
# 数据库文件路径（默认：./data）
# DATABASE_PATH=./data

# 数据库连接 URL（切换到 PostgreSQL 时使用）
# DATABASE_URL=postgresql://user:password@localhost/vividcrowd

# -------------------- 模型配置（可选）--------------------
# 主回复模型（默认：qwen-max）
# 可选：qwen-max, qwen-plus, qwen-turbo
# MODEL_NAME=qwen-max

# 路由模型（默认：qwen-turbo）
# 建议使用 turbo 降低成本
# ROUTER_MODEL_NAME=qwen-turbo

# 评价模型（默认：qwen-plus）
# 用于销售演练评价
# EVALUATION_MODEL=qwen-plus

# -------------------- 群聊配置（可选）--------------------
# 严格人设检查（默认：True）
# STRICT_PERSONA_CHECK=True

# LLM 语义路由（默认：True）
# ENABLE_LLM_ROUTING=True

# 最小打字延迟（秒，默认：8.0）
# MIN_TYPING_DELAY=8.0

# 最大打字延迟（秒，默认：10.0）
# MAX_TYPING_DELAY=10.0

# 每轮最多回复人数（默认：3）
# MAX_AGENTS_PER_ROUND=3

# 深夜模式开始时间（小时，默认：23）
# NIGHT_MODE_START_HOUR=23

# 深夜模式结束时间（小时，默认：7）
# NIGHT_MODE_END_HOUR=7

# 深夜活跃概率（默认：0.2）
# NIGHT_MODE_PROBABILITY=0.2

# -------------------- 客服配置（可选）--------------------
# 高置信度阈值（默认：0.9）
# HIGH_CONFIDENCE_THRESHOLD=0.9

# 中置信度阈值（默认：0.6）
# MID_CONFIDENCE_THRESHOLD=0.6

# BM25 权重（默认：0.6）
# BM25_WEIGHT=0.6

# Embedding 权重（默认：0.4）
# EMBEDDING_WEIGHT=0.4
